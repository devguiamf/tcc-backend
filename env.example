# ===========================================
# Backend - Variáveis de Ambiente
# ===========================================
# Para desenvolvimento local: cp env.example .env
# Para Docker: variáveis vêm do docker-compose.yml
# ===========================================

# Ambiente
NODE_ENV=development

# Banco de Dados
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=agendoo_user
DB_PASSWORD=agendoo_password
DB_DATABASE=agendoo

# JWT
JWT_SECRET=dev-secret-change-in-production
JWT_EXPIRES_IN=7d

# Email SMTP (para recuperação de senha)
# Gmail: Use "Senha de App" em https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASS=sua-senha-de-app
SMTP_FROM="Agendoo <noreply@agendoo.com>"

# URL do Frontend (para links de email)
FRONTEND_URL=http://localhost:4200

